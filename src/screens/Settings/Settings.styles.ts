import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  optionsContainer: {
    marginHorizontal: 15
  },
  cardsContainer: {
    marginBottom: 20
  }
});

export default styles;
