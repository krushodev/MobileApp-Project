import { View, Text } from 'react-native'
import RoomsListContainer from '../../components/RoomsListContainer/RoomsListContainer';

const Rooms = () => {
  return (
    <View style={{flex: 1}}>
      <RoomsListContainer/>
    </View>
  )
}

export default Rooms;
