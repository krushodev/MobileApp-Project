import { View } from "react-native";
import { RoomsListContainer } from "../../components";

const Rooms = () => {

  return (
    <View style={{ flex: 1 }}>
      <RoomsListContainer />
    </View>
  );
};

export default Rooms;
