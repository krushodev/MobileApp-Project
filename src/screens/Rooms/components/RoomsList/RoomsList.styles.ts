import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    zIndex: -10
  },
  textAlert: {
    textAlign: 'center',
    padding: 20
  }
});

export default styles;
