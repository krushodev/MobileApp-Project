import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    zIndex: -10
  },
  textAlert: {
    fontSize: 25,
    textAlign: 'center',
    padding: 20
  }
});

export default styles;
