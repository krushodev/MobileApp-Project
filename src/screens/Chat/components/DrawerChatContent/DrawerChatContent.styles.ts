import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    textAlign: 'center',
    margin: 15,
    marginBottom: 5
  },
  membersList: {
    margin: 15
  }
});

export default styles;
