import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabBar: {
    height: 90,
    maxHeight: 110,
    margin: 10,
    borderRadius: 15
  },
  tabBarIconContainer: {
    alignItems: 'center',
    gap: 5
  }
});

export default styles;
