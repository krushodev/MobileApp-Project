import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    position: "absolute",
    right: 25,
    bottom: 30,
    padding: 8,
    zIndex: -10,
  },
});

export default styles;
