import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
    padding: 20,
    marginBottom: 10,
  },
  text: {
    fontSize: 20
  }
});

export default styles;
