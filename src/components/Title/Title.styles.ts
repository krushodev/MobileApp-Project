import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginTop: 20,
    marginBottom: 15
  },
  title: {
    marginLeft: 15,
    padding: 15
  }
});

export default styles;
